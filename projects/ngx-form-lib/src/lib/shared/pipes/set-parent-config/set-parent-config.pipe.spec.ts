import { SetParentConfigPipe } from './set-parent-config.pipe';

describe('SetParentConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new SetParentConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
